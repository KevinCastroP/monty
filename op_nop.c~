#include "monty.h"
/**
 * op_nop - doing nothing
 * @stack: pointer to list
 * @line_number: # of line
 * Return: nothing
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)(stack);
	(void)(line_number);
}
